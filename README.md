# Figures for the enrichment + UMAP use case

Figures generated by `examples/scRNAseq_enrichment` on the 10x Genomics public
PBMC dataset (3,733 cells after QC, 13 Leiden clusters).

This branch only holds images, so the feature branch stays free of binaries.
